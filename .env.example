# ───────────── Web (apps/web) ─────────────
NEXTAUTH_URL=""
NEXT_PUBLIC_API_URL=""
NEXTAUTH_SECRET=""

# OAuth providers
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# ───────────── Server (apps/server) ─────────────
NODE_ENV="development"
SERVER_PORT="8080"
WEB_ORIGIN="http://localhost:3000"

# Each must be at least 32 chars. Generate with: openssl rand -base64 48
JWT_ACCESS_SECRET=""
JWT_REFRESH_SECRET=""

ACCESS_TOKEN_TTL_SEC="900"
REFRESH_TOKEN_TTL_SEC="604800"

# ───────────── Database (packages/database) ─────────────
DATABASE_URL="postgresql://user:password@localhost:5432/orderbook_db"

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

KAFKA_BROKER="localhost:9092"
